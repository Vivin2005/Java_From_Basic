public class Array01 {
    public static void main(String[] args){
        // Array Concept.need of an array.
        // Array can be used to stored any kind of data.
        // Array can be store N numbers of data inside it.
        // Also we can store the data in a variable but variable contain's only one value.
        // But in array we can store the many values(data).

        int i = 10; // Normal Variables
        int j = 20;
        int k = 30;

        // The array variables are the variable that can store multiple values.
        // We have to mention the ([]) square bracket.
        // The square brackets are mentioned after datatype name or after the variable name.
        int num[] = {5,6,7};

        // If we want to create the array variable.
        int num1[] = new int[4];
        // Here we are having the fixed array value only store 4 values inside the array.
        // It can hold only 4 values inside the num1 array variable.
    }
}
