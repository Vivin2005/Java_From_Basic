public class Hello01{
    public static void main(String[] args){
        /* 
        -- Yes other languages are good but. 
        -- When it comes to build a big project. java is only suitable.
        -- Because we can develop a project in a structured way.
        */

        /*
         -- Jshell is used to execute the java code line by line.
         -- Means in jshell we don't follow any kind of structure way to write java code
         -- It was introduced in Java 9. Go to cmd just print "jshell" to start with it. 
         */

         /*
          -- If we want to do something we need "methods".
          -- If we want to print a message in the output screen we need a print method.
          */

        // Printing something on the output screen.
        System.out.println("Hello, World!");
        System.out.println(6);
        System.out.println(6+10);
        // We can print anything what we want.
    }
}