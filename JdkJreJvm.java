public class JdkJreJvm {
    public static void main(String[] args){
        /*
          -- We can compile our code by using the JDK.
          -- That code will converted into a Byte code.
          -- That byte code will run in the JVM.
          -- In JRE jave the inbuild classess on it.the inbuild class,methods,etc we used inside our program.
          -- In JVM we execute everything.
         */
    }
}
